package com.example.diegoeg.ap.udenar.calculator;

import android.util.Log;

//import javax.script.ScriptEngineManager;
//import javax.script.ScriptEngine;
//import javax.script.ScriptException;
public class Calcular {

/*
    private ScriptEngineManager scriptEngineManager;
    private ScriptEngine scriptEngine;



    public static String resolver(String cadena){
        ScriptEngineManager mgr = new ScriptEngineManager();
        ScriptEngine engine = mgr.getEngineByName("JavaScript");
        String resultado = "";
        try {
            //Log.i("VAL EVAL","El VALOR ES "+engine.eval("2*2"));
            resultado = engine.eval(cadena).toString();
            //Double res = (Double) r;
            //resultado = res;
        } catch (ScriptException e) {
            //e.printStackTrace();
            Log.e("ENGINE ERROR ","DE NUEVO FALLA EL ENGINE");

        }
        finally {
            return resultado.toString();
        }


    }


*/


}
